function music(name, title) {
    // return name + ' - ' + title; // name - title
    return `${name} - ${title}`;
}

console.log(music('윤도현', '흰수염고래'));

function muzic(name, title) {
    return `${name} - ${title}`;
}

console.log(muzic('로제', 'Gameboy'));

function mujic(name, title) {
    console.log(`${name} - ${title}`);
}

console.log(mujic('로제', 'Gameboy'));

function square(n) {
    return`${n}**`
}

square(4);
